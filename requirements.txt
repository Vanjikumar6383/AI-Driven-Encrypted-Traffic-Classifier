# Core ML Stack
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
joblib>=1.3.0

# Visualization
matplotlib>=3.7.0
# (Chart.js is loaded via CDN in index.html)

# Network Analysis (Use Wireshark/TShark for capturing & metadata extraction)
scapy>=2.5.0
# (TShark is used via command line for live/pcap metadata extraction)

# Backend (Optional if running as a web service)
flask>=2.3.0
